import 'package:flutter/cupertino.dart';

class Help extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('수도권지하철 실시간 도착안내\n앱버전: 0.0.1'),
    );
  }
}
